export const metadata = {
  title: "Admin Portal | Carpenterwala",
  robots: {
    index: false,
    follow: false,
    nocache: true,
  },
};

export default function AdminLayout({ children }) {
  return children;
}
